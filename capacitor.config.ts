import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nicolas.macrotracker',
  appName: 'MacroTracker AI',
  webDir: 'dist'
};

export default config;
